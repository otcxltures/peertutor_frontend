services:
  frontend:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:8000/api}
    container_name: peertutor_frontend
    restart: unless-stopped
    ports:
      - "5173:80"